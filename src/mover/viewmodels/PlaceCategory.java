package mover.viewmodels;

//this is not implicitly shared
//I decided here to use enum because it saves typing, for example
//We could have done this like this
// public static
public enum PlaceCategory {
	Pizzeria
}
